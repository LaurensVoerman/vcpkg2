From 69046e0a632449090bf79d843a0f65f6b76c8822 Mon Sep 17 00:00:00 2001
From: LaurensVoerman <l.voerman@rug.nl>
Date: Fri, 7 Nov 2025 16:58:47 +0100
Subject: [PATCH] viewerSDLstatic

---
 examples/osgviewerSDL/CMakeLists.txt | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/examples/osgviewerSDL/CMakeLists.txt b/examples/osgviewerSDL/CMakeLists.txt
index 8498958..445658f 100644
--- a/examples/osgviewerSDL/CMakeLists.txt
+++ b/examples/osgviewerSDL/CMakeLists.txt
@@ -16,7 +16,11 @@ ELSE()
                SET(TARGET_EXTERNAL_LIBRARIES ${SDLMAIN_LIBRARY} ${TARGET_EXTERNAL_LIBRARIES} )
        ENDIF()
 ENDIF()
-
+IF(WIN32)
+  if(VCPKG_LIBRARY_LINKAGE STREQUAL "static")
+    SET(TARGET_ADDED_LIBRARIES ${TARGET_ADDED_LIBRARIES} winmm dxguid)
+  ENDIF()
+ENDIF(WIN32)
 IF (MINGW)
     SET(TARGET_EXTERNAL_LIBRARIES mingw32 ${TARGET_EXTERNAL_LIBRARIES} winmm dinput ddraw dxguid)
 ENDIF()
-- 
2.51.0.windows.2

